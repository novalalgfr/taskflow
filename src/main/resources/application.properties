# Application Settings
app.name=TaskFlow Project Management
app.version=1.0.0

# Database Settings
db.host=localhost
db.port=3306
db.name=taskflow_db
db.username=root
db.password=

# Session Settings
session.timeout=30

# File Upload Settings
upload.max.size=5242880
upload.path=./uploads/

# Logging Level
logging.level=INFO